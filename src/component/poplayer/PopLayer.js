import config from '../config'
import Dom from '../dom/Dom'

let level = config.popLayerLevel;

/**
 * 自动弹出层
 * 检测内容的大小，选择下拉弹出或者浮动弹窗
 */
class PopLayer {
    constructor(parent, content) {
        
    }
}